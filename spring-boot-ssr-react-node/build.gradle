plugins {
    id "com.moowork.node" version "1.1.1"
}

apply plugin: 'base'

task publish(type: YarnTask) {
    args = ['publish']
}

check.dependsOn(yarn_install)
//bundle.dependsOn(npm_install)
//assemble.dependsOn(bundle)
